/*
 * InquiryPage Messages
 *
 * This contains all the text for the InquiryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.InquiryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InquiryPage container!',
  },
});
